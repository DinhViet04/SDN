<%- include('partials/header', { title: 'Home' }) %>
    <%- include('partials/requireAuth') %>

        <div style="text-align:center; padding: 5rem 2rem;">
            <div style="font-size:4rem; margin-bottom:1rem;">🚗</div>
            <h1 style="font-size:2rem; font-weight:700; margin-bottom:0.75rem;">Viet Car Rental</h1>
            <p style="color:var(--muted); margin-bottom:2rem;">Manage your fleet and bookings with ease.</p>
            <div style="display:flex; gap:1rem; justify-content:center;">
                <a href="/cars" class="btn btn-primary">🚙 View Cars</a>
                <a href="/bookings" class="btn btn-ghost">📋 View Bookings</a>
            </div>
        </div>

        <%- include('partials/footer') %>