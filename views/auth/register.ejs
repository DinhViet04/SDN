<%- include('../partials/header', { title: 'Đăng ký' }) %>

    <div class="auth-wrapper">
        <div class="auth-card">

            <!-- Logo -->
            <div class="auth-logo">
                <span class="auth-logo-icon">🚗</span>
                <span class="auth-logo-text">CarRental</span>
            </div>

            <h1 class="auth-title">Tạo tài khoản</h1>
            <p class="auth-sub">Đăng ký để bắt đầu quản lý xe & đặt chỗ</p>

            <div id="alertBox" class="alert alert-danger" style="display:none"></div>

            <form id="registerForm" class="form-grid" novalidate>
                <div class="form-group">
                    <label for="username">Tên đăng nhập</label>
                    <input id="username" type="text" placeholder="vd: nguyenvana" required autocomplete="username"
                        minlength="3" />
                </div>

                <div class="form-group">
                    <label for="email">Email</label>
                    <input id="email" type="email" placeholder="vd: user@example.com" required autocomplete="email" />
                </div>

                <div class="form-group">
                    <label for="password">Mật khẩu <span style="color:var(--muted); font-weight:400">(ít nhất 6 ký
                            tự)</span></label>
                    <div class="input-wrapper">
                        <input id="password" type="password" placeholder="••••••••" required minlength="6"
                            autocomplete="new-password" />
                        <button type="button" class="eye-btn" onclick="togglePass('password', this)">👁</button>
                    </div>
                </div>


                <button type="submit" class="btn btn-primary btn-full" id="submitBtn">
                    <span id="btnText">Đăng ký</span>
                    <span id="btnSpinner" class="spinner" style="display:none">⏳</span>
                </button>
            </form>

            <p class="auth-footer-text">
                Đã có tài khoản? <a href="/auth/login">Đăng nhập</a>
            </p>
        </div>
    </div>

    <style>
        main {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: calc(100vh - 130px);
            padding: 2rem;
        }

        .auth-wrapper {
            width: 100%;
            max-width: 440px;
        }

        .auth-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 2.5rem 2rem;
            box-shadow: 0 24px 64px rgba(0, 0, 0, .4);
            animation: slideUp .35s ease;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(24px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .auth-logo {
            display: flex;
            align-items: center;
            gap: .6rem;
            margin-bottom: 1.75rem;
        }

        .auth-logo-icon {
            font-size: 2rem;
        }

        .auth-logo-text {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--accent2);
        }

        .auth-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: .4rem;
        }

        .auth-sub {
            color: var(--muted);
            font-size: .9rem;
            margin-bottom: 1.75rem;
        }

        .input-wrapper {
            position: relative;
        }

        .input-wrapper input {
            width: 100%;
            padding-right: 3rem;
        }

        .eye-btn {
            position: absolute;
            right: .75rem;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            color: var(--muted);
            transition: color .2s;
        }

        .eye-btn:hover {
            color: var(--accent2);
        }

        .btn-full {
            width: 100%;
            justify-content: center;
            padding: .75rem;
            font-size: 1rem;
            border-radius: 10px;
        }

        .btn-full:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(108, 99, 255, .35);
        }

        .auth-footer-text {
            text-align: center;
            margin-top: 1.5rem;
            font-size: .875rem;
            color: var(--muted);
        }

        .auth-footer-text a {
            color: var(--accent2);
            text-decoration: none;
            font-weight: 600;
        }

        .auth-footer-text a:hover {
            text-decoration: underline;
        }

        .spinner {
            display: inline-block;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }
    </style>

    <script>
        function togglePass(id, btn) {
            const inp = document.getElementById(id);
            inp.type = inp.type === 'password' ? 'text' : 'password';
            btn.textContent = inp.type === 'password' ? '👁' : '🙈';
        }

        function showAlert(msg) {
            const box = document.getElementById('alertBox');
            box.textContent = msg;
            box.style.display = 'block';
            box.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        }

        document.getElementById('registerForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const btn = document.getElementById('submitBtn');
            const btnText = document.getElementById('btnText');
            const spinner = document.getElementById('btnSpinner');
            const alertBox = document.getElementById('alertBox');

            alertBox.style.display = 'none';
            btnText.textContent = 'Đang đăng ký...';
            spinner.style.display = 'inline';
            btn.disabled = true;

            try {
                const res = await fetch('/api/auth/register', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        username: document.getElementById('username').value.trim(),
                        email: document.getElementById('email').value.trim(),
                        password: document.getElementById('password').value,
                    }),
                });

                const data = await res.json();

                if (!res.ok) {
                    showAlert(data.error || 'Đăng ký thất bại');
                    return;
                }

                localStorage.setItem('token', data.token);
                localStorage.setItem('user', JSON.stringify(data.user));
                window.location.href = '/';
            } catch (err) {
                showAlert('Lỗi kết nối, vui lòng thử lại.');
            } finally {
                btnText.textContent = 'Đăng ký';
                spinner.style.display = 'none';
                btn.disabled = false;
            }
        });
    </script>

    <%- include('../partials/footer') %>